// collection of edge types used in app

import { EdgeTypes } from "@xyflow/react";
import AppEdge from "../../components/edges/AppEdge";

const edgeTypes: EdgeTypes = {
  "app-edge": AppEdge,
};

export default edgeTypes;
