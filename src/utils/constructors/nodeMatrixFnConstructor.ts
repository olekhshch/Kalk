import { NodeType } from "../../types/nodes";

// constructs node with functions that produce matrices (...num) => matrix

const nodeMatrixFnConstructor = () => {};

export default nodeMatrixFnConstructor;
