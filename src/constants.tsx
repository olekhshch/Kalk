import React from "react";
import ReactDOM from "react-dom/client";
import ConstantsWindow from "./layout/UI/Contants/ConstantsWindow";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <ConstantsWindow />
  </React.StrictMode>
);
